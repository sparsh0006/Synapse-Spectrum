# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build output
dist/
build/
out/
coverage/
*.tgz

# TypeScript
*.tsbuildinfo
# If you were outputting JS files alongside TS (not typical with bundlers like Vite/Webpack)
# *.js
# *.js.map
# *.jsx

# Webpack (though you're using Vite, these are common for JS projects)
# .webpack/
# webpack-bundle-analyzer-report.html

# Vite
.vite/
# vite.config.js.timestamp # Older Vite versions might create this

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (not used in this project, but good general practice)
.grunt

# Bower dependency directory (not used in this project)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (not used in this project)
build/Release

# Dependency directories
# node_modules/ # Already covered above

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Microbundle cache
.mb_cache/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarnclean

# IDE / Editor specific files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# macOS
.DS_Store
.AppleDouble
.LSOverride
# Thumbnails
._*
# Files that might appear in the root of a volume
.Spotlight-V100
.Trashes
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Optional ssh private keys (NEVER commit private keys)
# .ssh/id_rsa
# .ssh/id_ed25519

# D3 (no specific ignore needed unless you generate local data files)

# Three.js (no specific ignore needed unless you cache textures/models locally outside assets)

# html2canvas (no specific ignore needed)

# Test snapshots (if you add Jest or Vitest)
__snapshots__/